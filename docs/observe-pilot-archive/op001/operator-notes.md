# op001 — NO HUMAN OPERATOR (runner-self-pilot warm-up)

**This file does NOT contain operator-behaviour notes.** No human
operator was present to be observed. See `feedback.md` for the full
disclaimer.

What this archive replaces this file with:

- **Setup-side runner notes** are in `usability-findings.md`
  (clearly attributed to the runner, not to an absent operator).
- **Factual ingestion data** is in `telemetry-summary.md`.
- **Verbatim engine output** is in `digest.md`.

The next pilot (`op002`) — first real engineer — will fill this
file with real observations of the engineer's behaviour while they
read the digest. That is the file's actual purpose.
