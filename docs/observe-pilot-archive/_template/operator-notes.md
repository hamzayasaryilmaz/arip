# Operator notes — `op<id>`

_Filed by the pilot runner, not the operator. Observations of
behaviour, NOT the operator's own words. Keep verbatim quotes for
`feedback.md`._

## Session metadata

- Pilot ID:          `op<id>`
- Date:              `YYYY-MM-DD`
- Pilot runner:      `<initials>`
- Operator role:     `<senior backend? SRE? QA lead? team lead?>`
- Operator's repo:   `<one-line description; no internal name needed>`
- Time-to-first-digest: `<minutes>`

## First five minutes

What did the operator do in the first five minutes after the digest
was opened? List the actions in order, no interpretation:

1. ...
2. ...
3. ...

## Eye-tracking proxies

Where did their attention go? (You don't need an eye-tracker; just
note what they pointed at or scrolled to.)

- First thing read:   `<run summary? rule clusters? abstentions?>`
- Most-re-read column: `<recurrence? services? operations?>`
- Skipped sections:   `<list>`
- Section they spent the longest on: `<which one and roughly how long>`

## Hesitations and double-takes

Each entry: what the operator did, and what they had to figure out
to continue.

> e.g. "Stopped on the `quality_band` column. Asked 'is medium good
>  or bad?' Took 30s of reading OBSERVE_MODE.md to resolve."

## Tool jumps

Did the operator open another tool to verify or cross-check? Which
tool, when, why?

- `<tool>` opened at `<approximate moment>` to `<purpose>`

## Body-language signals

- Looked confused at: `<...>`
- Looked relieved at: `<...>`
- Looked frustrated at: `<...>`
- Looked engaged at:  `<...>`

If none of these applied, say so honestly: *"Neutral throughout."*

## Closing reaction

How did the operator end the session — engaged, drained, neutral?
What was their last action before getting up from the desk?
